# E3D MCP 完善 · 真机验收执行单（2026-07-02）

> 按《E3D_MCP_完善指导书》附录 D 顺序，P0 → P1 → P3(机械) → P2① → P3-3/4 + N1/N2/N3 已全部在代码层落地，
> `dotnet build -c Release` **0 警告 0 错误**（本机无真 E3D:8286，编译=本机验收代理）。
> 本单是附录 C 的真机执行清单：把新 DLL 部署到 E3D 机后逐项跑，**⚠ 项不过不算完成**。

## 0. 部署

1. 拷 `C:\项目\17-E3D插件\bin\Release\net472\MCPServer.dll` 覆盖 E3D 机插件目录里的同名文件（DesignAddins.xml 指向的路径）。
2. 重启 E3D。看 `D:\pipingclaw_e3d_pml.log`（无 D 盘则 `%TEMP%\pipingclaw_e3d_pml.log`）应出现：
   - `SRV  MCP token ready（%TEMP%\mcp_token.txt…）`
   - `SRV  MCP HTTP server started on :8286`
3. **Token（P1 新增）**：插件启动时生成随机 token 写入 E3D 机 `%TEMP%\mcp_token.txt`；要固定 token 就设环境变量 `E3D_MCP_TOKEN` 再启 E3D。
   09 中间件已改为**自动**取 env `E3D_MCP_TOKEN` 或 `%TEMP%\mcp_token.txt` 并带 `X-MCP-Token` 头 —— **中间件 python 需重启一次**。
   （中间件与 E3D 不同机时：把 E3D 机的 token 值设到中间件机的 `E3D_MCP_TOKEN` 环境变量。）

## 1. P1-C 安全验收（4 条，E3D 机 PowerShell）

```powershell
$tok = Get-Content "$env:TEMP\mcp_token.txt"
# ① 无 token → 401
Invoke-WebRequest -Uri http://localhost:8286/tools -UseBasicParsing -SkipHttpErrorCheck | Select StatusCode   # 期望 401
# ② 带 token → 200
Invoke-WebRequest -Uri http://localhost:8286/tools -Headers @{ 'X-MCP-Token' = $tok } -UseBasicParsing | Select StatusCode  # 期望 200
# ③ 跨源 → 403（模拟浏览器带外部 Origin）
Invoke-WebRequest -Uri http://localhost:8286/tools -Headers @{ Origin = 'http://evil.example'; 'X-MCP-Token' = $tok } -UseBasicParsing -SkipHttpErrorCheck | Select StatusCode  # 期望 403
# ④ /health 免 token → 200
Invoke-WebRequest -Uri http://localhost:8286/health -UseBasicParsing | Select StatusCode  # 期望 200
```
（PS 5.1 无 -SkipHttpErrorCheck 时用 `try { … } catch { $_.Exception.Response.StatusCode }` 读状态码。）

危险门两条（经 09 chat 或 Doctor 🛠 工具调试直调）：
- `e3d_element_delete(name=/任意测试件)` 不带 confirm → 返回 `Error: … 危险操作：请…带 confirm="true" 重试`；带 `confirm:"true"` → 执行。
- `e3d_pml_exec("DELETE ZONE /X")` → 被逃生舱黑名单拒 + 引导话术（DELETE/PURGE/UNCLAIM ALL/FINISH/QUIT 同）。
- 09 侧 F2 写回闸门批准后**自动补 confirm**（同一次写不会被问两次）——走一次正常写回流程确认无回归。

## 2. P0-D 读回通道验收（⚠ 核心）

1. `e3d_pml_eval("!!CE.NAME")` 与 `e3d_pml_eval("(HBORE OF /某分支)")` **连续 20 次**：应无一次空读（旧版嵌套作用域局部变量会间歇读空）。
2. 故意失败：对不存在元素 `e3d_pml_exec("Q ATT /NOT_EXIST_XXX")` → 返回里应含 **E3D 真实错误文本**，而不是"查日志/check console"。
3. **⚠ .Result 三命令比对**：`e3d_pml_exec` 各跑一次 `Q ATT`、`Q ITLE`、`Q MEM`，与 E3D 命令窗输出比对。
   - `.Result` 空时代码自动回退 `VAR !!MCPQn = <命令>` 赋值式读回——tail 日志确认走了哪条、值对不对。
4. **⚠ 形态裁决**（都记录进日志，逐字看）：
   - 赋值走 `RunPMLCommandInPDMS`；若真机报错，改回 `RunPMLCommand`（[E3dApiWrapper.cs](Tools/E3dApiWrapper.cs) `EvalViaPmlGlobal`/`ReadPmlViaGlobal` 两处）。
   - `GetStringFromPML` 传的是带 `!!` 前缀的变量名（指导书口径）；读空则试去前缀。
   - 表达式直赋 `!!x = expr` 失败会自动再试 `VAR !!x = expr`（EvaluatePml 双形态兜底）——确认至少一种成立。

## 3. P2 建模验收

- 五管件工具（valve/flange/tee/bend/reducer）schema 已说真话（at 不生效、Choose 建件、定位用 pos_dist/pos_thr/connect 微调）。跑附录 C 端到端标准题：
  > 从零建 DN100 水管：SITE→ZONE→PIPE→BRANCH（设 SPRE、头尾 HPOS/TPOS/HDIR/TDIR）→ 头法兰→弯头(东转北)→三通(支管朝上)→弯头→尾法兰，逐件 CONN，`Q ITLE` 全程无负值，人工确认后 `e3d_db_save(confirm="true")`。
  > 通过标准：Agent 声称的每个动作与日志/模型一致（无"以为定位了"的幻觉步骤）；失败步骤 Agent 能凭带内错误自愈重试。
- ⚠ CUT/GAP/JOIN/ROUTE 四个裸命令工具已标"未经真机验证"——真机各试一次，被拒的在 schema 里降级为"未实现"或改造。
- **P2②（at 真落地）真机实验**：在测试分支上依次试 `Dist 3500`、`THRO PH`、`CONN`，取被接受且几何正确者写进 InsertComponent（本轮只做了①说真话，②留真机）。

## 4. P3 / N 系验收

- **P3-3/4**：在结构树上快速连点 CE ~20 次 → E3D UI 不卡（广播已挪后台线程）；09 侧只收到最后一次 `ce_changed`（200ms 合并）。
- **N1**：`e3d_attr_set(attr=HPOS, value="乱写的串")` → 立即返回 `Error: HPOS 坐标/朝向串非法（未发往 E3D）`（不再静默失败）；`"E 1000 N 2000 U 500"` 正常；含 `WRT` 的串跳过预校验直接走 PML（⚠ Parse 是否认 WRT 由真机裁决，若认可去掉跳过逻辑）。
- **N2**：`e3d_clash_check(/某管)` → 返回若带 `(官方 Clasher API)` 字头 = API 通道成功（结构化四类清单）；若走了旧 PML 报告 = 实现类探测失败（tail 日志看 `[Clasher API 失败，回退 PML]` 原因），把日志发回来再修。
- **N3**：换 MDB/项目 → 09 侧应收到 `notifications/e3d/mdb_opened` / `mdb_closing`。

## 5. 指导书 vs 本机 DLL 的出入（已按 DLL 为准实现，供复核）

| 指导书说 | 本机 lib DLL 实况（元数据 dump） | 采用 |
|---|---|---|
| `cmd.RunPMLCommand(...)` 实例方法 | **静态**方法；且有 `RunPMLCommandInPDMS` | 静态 + InPDMS（⚠真机裁决） |
| `GetStringFromPML(string, out, out)` | 静态 + **out**（第一轮实测编译器要求） | 静态 + out |
| `DeletePMLVar` 可用 | 是 **internal**（编译不可达） | 改 PML `VAR !!x DELETE` 自删 |
| `DbEvents.AddMdbOpenedEventHandler` | 不存在；实为 `DatabaseService.MDBOpened/MDBClosing` 静态事件 | DatabaseService |
| P3-2 `MDB.FindElement(string)` 全库替换 | FindElement 是 MDB **实例**方法，而 DLL 无任何 CurrentMDB 静态访问器 | **暂缓**（拿不到实例=不可安全兑现；GetElement 真机读路径已实证可用） |
| N2 `Clasher.CheckAll(...)` 直接用 | Clasher 是**抽象类** | 运行时探测 Implementation 子类，失败回退 PML |

## 6. 回归清单

P0-D + P1-C + P2-D 各项 + 第零章五处未破坏：主线程调度（未动）、RunWrite 读写分离（保留，只加了带内错误）、Choose 建模范式（未动）、POS 走 PML 主通道（N1 只加前置校验）、PML 日志闭环（保留 + 扩 SRV 事件）。

## 7. P2-C 全量巡检结果（3 分片并行审计 96 工具）

**已修（代码级，本轮）**：8 个假成功工具改真判成败（db_undo/db_extract/element_revert/array_sort/db_claim/db_release/draft_iso[+输出文件回查]/attr_copy 全走 RunWrite）；pml_exec 失败前缀 FAILED→Error（修双层穿透假成功）；五管件 handler at 改可选（消 schema/handler 矛盾）；support_create 真写 STYPE、label_create 文本写失败并进回执；e3d_wrt 真读回值；export_rvm 带内错误（LastExportError）；事件总线接通（ce_changed/db_changed 真喂 E3dEventBus.Publish → e3d_poll_events 不再恒 0）；attr_batch_set_multi 全败时 IsError=true。

**已知未修（描述漂移，留下一轮或按需）**：
- e3d_navigate 的 OWNER/MEMBERS 相对导航描述的功能不存在（实现只认绝对路径）
- e3d_search/e3d_collect 的 filter 解析失败时静默当全匹配（结果标头有误导）
- e3d_attr_list "所有属性"实为 ~40 项白名单
- e3d_element_move 走 MOVE…TO（本仓自判"错路"语法，未标 ⚠）
- e3d_spec_query 恒回"Queried"，spec 不存在也不报错（建模选型链慎用，取数走 e3d_attr_read SPREF + Q SPCO）
- e3d_pipe_slope_check 把"坡度大于目标"也计入违规（判定应为 slope < min−tolerance 才违规）；三个管道分析工具传 PIPE 只读到 BRAN 层（应传 BRAN 或下钻）
- e3d_element_batch_create 描述承诺的 SkillRuntime 回滚在 09 侧未登记可逆性（回滚会 HALT）
- 返回样例过时一批（element_create/bom/db_changes——实现比描述好，样例待更新）

**改库类无写后回查名单**（返回"命令已接受"级而非"写后状态"级，做附录 C 端到端题时注意用 Q ATT 复核）：attr_set/batch_set/element_rename/copy/move/pos_set/pos_move/rotate/reverse/connect/disconnect/array_add/remove/pipe 系全部。
